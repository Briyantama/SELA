# Copy to .env for local development. Never commit .env.
# Placeholder values only; use your own local-only values.
POSTGRES_USER=sela
POSTGRES_PASSWORD=change-me-local-only
POSTGRES_DB=sela
REDIS_PASSWORD=change-me-local-only
